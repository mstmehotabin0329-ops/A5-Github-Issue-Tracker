
  - 1️⃣ What is the difference between var, let, and const?
    Answer:var is a function scoped.
    let and const are block scoped . const value can't be reassigned.
  
  - 2️⃣ What is the spread operator (...)?
    Answer: spread operator (...) is used to copy or merge arrays and objects.

  - 3️⃣ What is the difference between map(), filter(), and forEach()?
    Answer: map() return new array. filter() return filterd array. and forEach () return only loop
  - 4️⃣ What is an arrow function?
    Answer: arrow function is a shorter syntax of writing functions using => .
  - 5️⃣ What are template literals?
    Answer: template literals 

